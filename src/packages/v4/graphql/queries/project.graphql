query Projects($where: Project_filter, $first: Int, $skip: Int) {
  projects(where: $where, first: $first, skip: $skip) {
    projectId
    metadata
    handle
    contributorsCount
    createdAt
  }
}