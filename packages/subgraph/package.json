{
  "name": "@juice.work/subgraph",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ @peripheralist/juice",
    "create-local": "graph create --node http://localhost:8020/ juice-local",
    "remove-local": "graph remove --node http://localhost:8020/ juice-local",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 juice-local"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.20.0",
    "@graphprotocol/graph-ts": "^0.20.0"
  },
  "devDependencies": {
    "mustache": "^3.1.0"
  }
}
