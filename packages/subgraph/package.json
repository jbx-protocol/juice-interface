{
  "name": "@jbox/subgraph",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020/ juicebox-local",
    "remove-local": "graph remove --node http://localhost:8020/ juicebox-local",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 juicebox-local",
    "deploy-rinkeby": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ @peripheralist/juicebox-rinkeby"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.21.0",
    "@graphprotocol/graph-ts": "^0.20.1"
  },
  "devDependencies": {
    "mustache": "^3.1.0"
  }
}
