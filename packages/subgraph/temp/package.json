{
  "name": "juicebox",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ juicebox",
    "create-local": "graph create --node http://localhost:8020/ juicebox",
    "remove-local": "graph remove --node http://localhost:8020/ juicebox",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 juicebox"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
