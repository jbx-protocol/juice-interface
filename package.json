{
    "name": "juice-frontend",
    "version": "1.0.0",
    "description": "Juicebox frontend monorepo",
    "repository": "https://github.com/jbx-protocol/juice-frontend",
    "license": "MIT",
    "private": true,
    "workspaces": [
      "./workspaces/*"
    ],
    "scripts": {
      "dev:interface": "yarn --cwd workspaces/interface dev",
      "build:interface": "yarn --cwd workspaces/interface build",
      "build": "yarn build:interface"
    }
  }
  